num = int(input("what is the number?: ")) #take input from the user
#func
def Factorial(num):
   
  for i in range(1, num):
    #print the factor that divides the number completely
    if num % i == 0 : #print those numbers which gives 0 as remainder  
      print(i)
  return num
print(Factorial(num))